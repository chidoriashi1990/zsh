# zsh

```sh
git clone https://github.com/chidoriashi1990/zsh.git
cp ~/zsh/zshrc ~/.zshrc
source ~/.zshrc
```

## starship

```sh
mkdir -p ~/.config
cp ~/zsh/starship.toml ~/.config/starship.toml
source ~/.zshrc  
```
